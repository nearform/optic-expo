name: publish

on:
  push:
    branches:
      - master

jobs:
  publish:
    name: deploy expo preview
    uses: nearform/optic-expo/.github/workflows/expo-publish.yml@master
    with:
      channel: default
    secrets:
      expo-token: ${{ secrets.EXPO_TOKEN }}